<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/layout"
    android:orientation="vertical"
    android:animateLayoutChanges="true"
    tools:context=".view.activity.ViewerActivity">

    <com.moaview.moaview_sdk.view.PinchZoomRecyclerView
        android:id="@+id/recyclerview"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <ImageButton
        android:id="@+id/bookmarkButton"
        android:layout_width="40dp"
        android:layout_height="60dp"
        android:layout_marginRight="25dp"
        android:layout_gravity="right"
        android:visibility="gone"
        android:background="@drawable/icon_bookmark_l"
        android:clickable="false"
        android:focusable="false" />

    <ImageButton
        android:id="@+id/bookmarkLeftButton"
        android:layout_width="40dp"
        android:layout_height="60dp"
        android:layout_marginLeft="25dp"
        android:layout_gravity="left"
        android:background="@drawable/icon_bookmark_l"
        android:visibility="gone"
        android:clickable="false"
        android:focusable="false" />

    <!-- TODO ssin :: 왜 버튼임?? 뷰로 바꿔-->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/toastButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/notify_first_page"
        android:padding="20dp"
        android:layout_marginLeft="20dp"
        android:layout_marginRight="20dp"
        app:cornerRadius="10dp"
        android:clickable="false"
        android:visibility="gone"
        android:backgroundTint="@color/black"
        android:textColor="@color/white"
        android:layout_gravity="bottom|center_horizontal" />
</FrameLayout>